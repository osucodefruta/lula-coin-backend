/* Este arquivo define a aparência do tabuleiro, peças, chat e todos os elementos visuais. */
body {
    font-family: 'Roboto', sans-serif;
    background-color: #333;
    color: #fff;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
}

#game-container {
    display: flex;
    gap: 20px;
}

#board-container {
    display: flex;
    flex-direction: column;
}

/* Estilo do tabuleiro. 'display: grid' é perfeito para criar o grid 8x8. */
#board {
    width: 640px;
    height: 640px;
    display: grid;
    grid-template-columns: repeat(8, 1fr); /* 8 colunas de tamanho igual. */
    border: 5px solid #8B4513;
    box-shadow: 0 0 20px rgba(0,0,0,0.5);
}

/* Estilo para cada casa (quadrado) do tabuleiro. */
.square {
    width: 80px;
    height: 80px;
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
}

.square.light { background-color: #F0D9B5; }
.square.dark { background-color: #B58863; }

/* Estilo para as peças de damas. */
.piece {
    width: 60px;
    height: 60px;
    border-radius: 50%; /* Deixa a peça redonda. */
    box-shadow: inset 0 -5px 5px rgba(0,0,0,0.3), 0 5px 5px rgba(0,0,0,0.3);
    cursor: pointer;
    transition: transform 0.2s ease;
}

.piece.red { background-color: #d81b1b; }
.piece.black { background-color: #222; }

/* Estilo para a "Dama" (peça coroada). */
.piece.king::after {
    content: '👑'; /* Adiciona o emoji de coroa. */
    font-size: 30px;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-shadow: 0 0 5px gold;
}

/* Destaques visuais para interatividade. */
.piece.selected {
    transform: scale(1.1); /* Aumenta a peça selecionada. */
    border: 3px solid #ffeb3b;
}

.valid-move::after {
    content: '';
    width: 20px;
    height: 20px;
    background-color: rgba(255, 255, 0, 0.5);
    border-radius: 50%;
    display: block;
}

/* Estilo do painel lateral (status e chat). */
#side-panel {
    width: 300px;
    background-color: #4a4a4a;
    padding: 20px;
    border-radius: 10px;
    display: flex;
    flex-direction: column;
    gap: 15px;
}
/* ... (Resto do CSS para player-info, chat-box, modais, etc., que definem cores, tamanhos e layout da interface) ... */
